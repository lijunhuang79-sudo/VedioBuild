# Netlify 环境变量示例（在 Netlify 后台 Build settings -> Environment variables 里添加）
# 复制需要的变量名与值，不设则前端会请求当前域名下的 /api-backend（需后端同域或 CORS）

# 后端 API 地址（必填，若后端单独部署）
NEXT_PUBLIC_API_URL=https://你的后端.railway.app

# 可选：若后端与前端同域并由 Nginx 反代，可留空或填当前站点 URL
